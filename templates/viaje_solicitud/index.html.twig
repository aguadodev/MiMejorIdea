{% extends 'base.html.twig' %}

{% block title %}ViajeSolicitud index{% endblock %}

{% block body %}
    <h1>ViajeSolicitud index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CreatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for viaje_solicitud in viaje_solicituds %}
            <tr>
                <td>{{ viaje_solicitud.id }}</td>
                <td>{{ viaje_solicitud.createdAt ? viaje_solicitud.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_viaje_solicitud_show', {'id': viaje_solicitud.id}) }}">show</a>
                    <a href="{{ path('app_viaje_solicitud_edit', {'id': viaje_solicitud.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_viaje_solicitud_new') }}">Create new</a>
{% endblock %}
