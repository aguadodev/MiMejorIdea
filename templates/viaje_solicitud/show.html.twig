{% extends 'base.html.twig' %}

{% block title %}ViajeSolicitud{% endblock %}

{% block body %}
    <h1>ViajeSolicitud</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ viaje_solicitud.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ viaje_solicitud.createdAt ? viaje_solicitud.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_viaje_solicitud_index') }}">back to list</a>

    <a href="{{ path('app_viaje_solicitud_edit', {'id': viaje_solicitud.id}) }}">edit</a>

    {{ include('viaje_solicitud/_delete_form.html.twig') }}
{% endblock %}
