{% extends 'base.html.twig' %}

{% block title %}Ingrediente{% endblock %}

{% block body %}
    <h1>Ingrediente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingrediente.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ ingrediente.nombre }}</td>
            </tr>
            <tr>
                <th>Notas</th>
                <td>{{ ingrediente.notas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ingrediente_index') }}">back to list</a>

    <a href="{{ path('app_ingrediente_edit', {'id': ingrediente.id}) }}">edit</a>

    {{ include('ingrediente/_delete_form.html.twig') }}
{% endblock %}
