{% extends 'base.html.twig' %}

{% block title %}Editar Viaje{% endblock %}

{% block body %}
    <h2>Editar Viaje</h2>

    {{ include('viaje/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('viaje/_delete_form.html.twig') }}

    <div>
	    <a href="{{ path('app_viaje_show', {'id': viaje.id}) }}" class="btn btn-secondary">Volver</a>
    </div>
    {{ include('snnipets/_alert_cambios_sin_guardar.html.twig') }}

{% endblock %}
